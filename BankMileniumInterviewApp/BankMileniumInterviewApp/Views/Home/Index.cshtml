@model BankMileniumInterviewApp.Models.UserInfo

@{
    ViewBag.Title = "Index";
}


@using (Html.BeginForm("Next", "Home"))
{
    @Html.Editor("Name", new { value=Model.Name})

    @Html.Editor("Email", new { value = Model.Email })

    <input type="submit" value="Dalej" />
    
}

